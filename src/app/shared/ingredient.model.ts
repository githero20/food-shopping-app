export class Ingredient {
  //   public name: string;
  //   public amount: number;

  //   getting rid of the declaration of properties and simply adding an accessor (public, private)
  // and the properties as parameters in the constructor

  constructor(public name: string, public amount: number) {}
}
